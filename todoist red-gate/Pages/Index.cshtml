@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="row">
    @if (!User.Identity.IsAuthenticated)
    {
        <a asp-action="login" asp-controller="trelloauthorization" class="btn btn-primary">Log In with Trello</a>

    }
    else
    {
        <p>You are logging</p>
    }
</div>